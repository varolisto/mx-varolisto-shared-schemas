---
---

Tooling-only: configura Biome 2 (lint + format), agrega flags estrictos al tsconfig y crea un workflow de CI que corre typecheck + lint + build en cada push/PR. Sin cambios en la API publicada del paquete — no requiere bump de versión.
