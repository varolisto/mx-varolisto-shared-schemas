/** @deprecated Sin uso interno. Se eliminará en el siguiente major. */
export const PROPOSITO_TOKEN = ['subir_documentos'] as const

/** @deprecated Sin uso interno. Se eliminará en el siguiente major. */
export type PropositoToken = (typeof PROPOSITO_TOKEN)[number]
